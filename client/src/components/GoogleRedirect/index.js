import React from 'react';
import { Jumbotron, Button } from 'reactstrap';

const index = () => {
    return (
        <div>
            <Jumbotron>
                <Button>Continue as a Landlord</Button>
                <Button>Continue as a Tenant</Button>
            </Jumbotron>
        </div>
    );
};

export default index;